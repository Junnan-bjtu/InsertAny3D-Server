# InsertAny3D 操作与验收索引（2026-08-16）

服务器项目：`${INSERTANY3D_SERVER_ROOT:-/path/to/InsertAny3D-Server}`

持久会话：`tmux codex_insert:0`。GPU/TRELLIS/GIM/SAGS 长任务均通过该会话
启动，stdout/stderr 写入 `codex_ops/*.log`。本文不记录任何真实 API key。

## 关键命令形态

```bash
# Gemini 离线契约测试（只使用 mock-secret）
third_party/TRELLIS/.venv/bin/python tools/test_gemini_edit.py

# 单任务自动分类并串联到 pose
third_party/TRELLIS/.venv/bin/python tools/run_insert_pipeline.py \
  --input-image <edited-center.png> \
  --run-root <server-root>/<scene-id> --task-id Task_001 \
  --task-prompt '<task prompt>' \
  --render-mode anchor \
  --scene-image <left.png>   --scene-image <center.png>   --scene-image <right.png> \
  --scene-depth <left.raw>   --scene-depth <center.raw>   --scene-depth <right.raw> \
  --scene-camera <left.camera.json> --scene-camera <center.camera.json> --scene-camera <right.camera.json>

# pose/编排器合成真值回归
third_party/gim/.venv/bin/python tools/test_estimate_similarity_pose.py
```

Unity 本地批处理：

```bat
python Tools\render_insert_workflow.py --stage anchor ^
  --scene Assets/GSTestScene.unity --output F:\InsertRuns\Farm_001

python Tools\render_insert_workflow.py --stage benchmark ^
  --scene Assets/GSTestScene.unity --output F:\InsertRuns\Farm_001 ^
  --pose-root F:\ServerRuns\Farm_001
```

## 可复盘结果

| 内容 | 结果 | 日志/产物 |
| --- | --- | --- |
| Gemini native 请求与图片响应 | `GEMINI_MOCK_READY`，exit 0 | `codex_ops/gemini_mock.log` |
| legacy 文本分割 | 1 mask / 1534 pixels | `codex_ops/task_prompt_segment.log` |
| TRELLIS PLY | `TRELLIS_ASSET_READY` | `codex_ops/trellis_asset.log` |
| 定向 -24/0/+24 渲染 | 3 张非空 RGB + absdepth | `codex_ops/pose_render_smoke_v2.log` |
| anchor 编排/GIM | 3 对均成功 | `codex_ops/anchor_pipeline_smoke.log` |
| 更新后 GIM JSON | 4831 点、4831 confidence | `codex_ops/gim_metadata_smoke.log` |
| pose 数学 + 编排器 | 20/20 内点，矩阵最大误差 8.32e-08 | `codex_ops/final_pose_pipeline_synthetic.log` |
| task prompt -> headless SAGS | 2433 vertices | `codex_ops/sags_pose_named.log` |
| Unity 相机 JSON | 结构化内外参 + radial depth 协议 | `MyProjects/Farm/CameraMetadataSmoke` |
| Unity step1 分类 | `Task_001` 下 left/center/right，实际 3/3 | `MyProjects/Farm/InsertTaskClassificationSmoke` |
| Unity pose 应用 + step6 | original/inserted 各 1，图片哈希不同 | `MyProjects/Farm/InsertPoseApplicationSmoke` |
| Unity 最终干净编译/渲染 | exit 0，无 C# error | `MyProjects/Farm/final-workflow-compile-check2.log` |
| 最终双环境编译 + 离线回归 | Gemini mock、pose 数学、任务目录编排全部 exit 0 | `codex_ops/final_acceptance_20260816.log` |

## 已知未完成的外部验证

- LangSAM 的 SAM2.1 small 权重直链约 176 MB，本次仅下载到约 1.1 MB，实测
  速度约 30--50 KB/s 后停止；`auto` 的 legacy 回退已经验证。
- 没有使用真实 APIYi key；外部网络、账号额度和模型名可用性未验收。
- pose 已通过可重复合成真值测试，真实生成物的 GIM 噪声、坐标轴和尺度仍需
  用首批人工检查任务标定。
- Unity 已能应用 pose，但服务端 PLY/GLB 自动创建插件专用资产尚未接入。
